# zephyr-docker-image
A Docker image to build Zephyr applications.

# Scripts to build and run locally
The `scripts` folder contains some handful scripts to build and run (open)
a bash session into the built Docker container.
